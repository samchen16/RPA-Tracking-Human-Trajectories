//
//  person.h
//  Person
//

#ifndef __person__
#define __person__

#include "point.h"
#include "opencv2/core/core.hpp"

class ClusterData {
private:
    Point position;
    cv::Mat color;  
    double person_confidence;

public:
    Person(Point post, cv::Mat col, double conf);
    ~Person();
};

#endif /* defined(__person__) */
